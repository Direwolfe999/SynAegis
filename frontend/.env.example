# Point frontend websocket to backend
NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:8080/ws/SynAegis

# Optional: disable by default to reduce quota burn
# true = send camera frames to backend/model, false = audio/text only
NEXT_PUBLIC_SEND_VIDEO_TO_AGENT=false
